<%= form_with(model: bet_position, class: "contents") do |form| %>
  <% if bet_position.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(bet_position.errors.count, "error") %> prohibited this bet_position from being saved:</h2>

      <ul class="list-disc ml-6">
        <% bet_position.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :bet_id %>
    <%= form.text_field :bet_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": bet_position.errors[:bet_id].none?, "border-red-400 focus:outline-red-600": bet_position.errors[:bet_id].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :driver_id %>
    <%= form.text_field :driver_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": bet_position.errors[:driver_id].none?, "border-red-400 focus:outline-red-600": bet_position.errors[:driver_id].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :position %>
    <%= form.number_field :position, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": bet_position.errors[:position].none?, "border-red-400 focus:outline-red-600": bet_position.errors[:position].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
